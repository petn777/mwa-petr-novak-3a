query getPosts {
  posts {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        databaseId
        title
        slug
        authorId
      }
    }
  }
}
